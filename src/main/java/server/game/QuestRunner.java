package main.java.server.game;

import main.java.server.game.pojo.Quest;
import main.java.server.state.Kingdom;


/**
 * Possibly could be considered the "Main Loop" of the program.  Runs the quest,
 * updates the world given the results of the quest, and tells the player what
 * happened and what to do next.
 */
public class QuestRunner {
    Kingdom kingdom;
    public void runQuest(Quest questToRun) {


        //Right now, assume they succeeded
//        kingdom.getQuests.remove(questToRun);
//        kingdom.addQuests(questToRun.getNextQuests());
        return;
    }
}
